App({
  onLaunch: function (e) {
    /**
     * 获取分享到群，用户在群里点击，获取群的id等信息，需要解密
     */
    // console.log('args',e)
    // wx.getShareInfo({
    //   shareTicket:e.shareTicket,
    //   success: function(res) {
    //     console.log('res', res)
    //   }
    // })
  }
})